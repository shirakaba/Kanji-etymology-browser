USERNAMES
jb15339
wk13290

SETUP
1) navigate to the 'container' folder and run the command 'npm install'.
2) run the command 'node index.js'.
3) open a browser at the URL: http://localhost:3000
4) search any single Japanese character (ie. the already-entered character), or click an example hyperlink from below.

FEATURES
See report.pdf for details.